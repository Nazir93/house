import { AdminCalculatorEditor } from "@/components/admin/admin-calculator-editor";

export const metadata = {
  title: "Калькулятор проектов — админка",
};

export default function AdminCalculatorPage() {
  return <AdminCalculatorEditor />;
}
