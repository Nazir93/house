# Подключать в http { } — см. scripts/setup-nginx-house.sh
limit_req_zone $binary_remote_addr zone=house_leads:10m rate=10r/m;
limit_req_zone $binary_remote_addr zone=house_auth:10m rate=30r/m;
limit_req_zone $binary_remote_addr zone=house_reviews:10m rate=10r/m;
